class AppConfig {
  static const appName = "Fover";
  static const baseUrl = "https://kyawsoemin.com";

  static const connectTimeout = 20;
  static const receiveTimeout = 20;
  static const sendTimeout = 20;
  static const retryAttempts = 2;
  static const retryDelayMillis = 500;
  static const appVersion = "1.0.0";
}