class TeamProfileHeaderLayout {
  const TeamProfileHeaderLayout._();

  static const double topPadding = 8.0;
  static const double bottomPadding = 12.0;
  static const double tabBarHeight = 38.0;
  static const double height = topPadding + bottomPadding + tabBarHeight;
}
